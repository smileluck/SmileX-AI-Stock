# === AI Model Configuration ===

# Required: Encryption key for storing API keys in the database
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=

# Optional: Bootstrap a default model before using the UI
LLM_DEFAULT_PROVIDER=
LLM_DEFAULT_MODEL=
LLM_DEFAULT_API_KEY=
LLM_DEFAULT_BASE_URL=
